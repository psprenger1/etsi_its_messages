Generated IVIM conversion headers live here.

Expected shape after successful generation:
- one `convert*.h` file per IVIM message in `etsi_its_msgs/etsi_its_ivim_ts_msgs/msg`
- root headers such as `convertIVIM.h`, `convertIviStructure.h`, `convertIviManagementContainer.h`, `convertIviContainer.h`

Run:

```bash
bash ../scripts/generate_conversion_headers.sh
```
